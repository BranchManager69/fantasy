############################################
# Fantasy League Engine – Example .env
# Copy to .env and customize for your setup
############################################

# ESPN authentication (cookies or credentials)
ESPN_LEAGUE_ID=
ESPN_SEASON=2025
ESPN_EMAIL=
ESPN_PASSWORD=
ESPN_S2=
ESPN_SWID=

# Data root (defaults to ./data)
# DATA_ROOT=/absolute/path/to/data

# Logging
# LOG_LEVEL=INFO

# Refresh scheduler tuning
# Game windows cadence (minutes)
FANTASY_REFRESH_GAME_INTERVAL_MINUTES=1
# Off-hours cadence (minutes)
FANTASY_REFRESH_IDLE_INTERVAL_MINUTES=60

# History retention (bump to preserve full SNF/MNF replay)
# Simulation snapshots to retain
FANTASY_REFRESH_MAX_SIM_HISTORY=240
# Weekly score snapshots per active week to retain
FANTASY_REFRESH_MAX_SCORE_HISTORY=360
# Diff log lines to retain (set 0 to disable pruning)
FANTASY_REFRESH_MAX_DIFF_LOG_LINES=50000

# Optional: refresh override windows (holidays, Black Friday, etc.)
# FANTASY_REFRESH_OVERRIDE_PATH=./config/refresh-overrides.json

# Frontend path discovery override (if running web outside repo)
# FANTASY_REPO_ROOT=/absolute/path/to/repo


